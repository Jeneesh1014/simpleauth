import {View, Text} from 'react-native';
import React, {memo} from 'react';

const Register = () => {
  return (
    <View>
      <Text>Register</Text>
    </View>
  );
};

export default memo(Register);
